$git clone <link del repo> 
il link si prende dalla repository su github web, versioni disponibili: https / ssh

$git add <nome file> 
i file vengono agggiunti ad una lista dei file da caricare nella repo

$git status 
mostra lo stato dei file locali rispetto alla repository remota

$git commit -m <messaggio> 
dice nel messaggio cosa hai modificato e perchè(tipo delle configurazioni)

$git push origin <nome del branch> 
serve per caricare i file nella repository remota

$git branch <nome del nuovo branch> <nome branch partenza> 
serve a creare nuovi branch

$git branch -d <nome del branch> 
serve per cancellare un branch dopo un merge

$git branch -D <nome del branch> 
server per cancellare un branch

$git checkout <nome del branch> 
serve per switchare al branch indicato

$git rm <nome del file> 
serve a rimuovere un file

$git pull origin <nome del branch> 
serve per scaricare file presenti nella repository remota

$git merge <nome del branch> 
serve per fondere il branch indicato con quello corrente

$git show 
mostra le ultime modifiche

$git log --pretty=oneline 
mostra tutti commit fatti con id

$git reset --hard <id> 
ritorna il commit selezionato senza possibilità  di tornare a tutti quelli che sono tra quello di partenza a quello in cui si è tornati

$git stash
inserisce i file modificati su un branch in uno "sgabuzzino" consentendo di muoversi temporaneamente su un altro branch senza dover mergiare le modifiche apportate per poter sistemare problemi o controllare cose e poi poter tornare nel proprio branch

$git stash pop
permette di ripristinare i file tenuti in caldo nello sgabuzzino sul proprio branch per poi procedere con un eventuale commit/merge

procedura per recuperare un file cancellato/modificato di cui si è fatto git add: 
$git status 
dovrebbe comparire in rosso una scritta che indica il file che è stato cancellato/modificato 
$git checkout -- <nome del file> 
ricrea il file com'era al momento dell'add
